language: rust
rust:
    - stable
    - beta
    - nightly
      #matrix:
      #env:
      # - TEST_DIR=/rust/mastering-rust/code/
        
allow_failures:
    - rust: nightly
script:
    - cd /rust/mastering-rust/code/
    - cargo test --verbose --all
